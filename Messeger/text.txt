To emit progress messages through the WebSocket from within `RequestAIResponseFunc`, you can pass the consumer's `send_msg` method as a callback and use it to send intermediate updates. Here's how to modify your code:

### Step 1: Pass `send_msg` to `RequestAIResponseFunc`
Modify the `handle_request_ai_response` method to include `send_progress` as an argument:

```python
async def handle_request_ai_response(self, prompt, email, NumberOfRequestRetry):
    # Pass self.send_msg as a progress callback
    val = await RequestAIResponseFunc(
        prompt=prompt,
        email=email,
        NumberOfRequestRetry=NumberOfRequestRetry,
        send_progress=self.send_msg  # Add this
    )
    await self.send_msg(data=val, type='RequestAIResponse')
```

### Step 2: Update `RequestAIResponseFunc`
Modify the function signature and emit progress messages at key points:

```python
@asyncCircuitBreaker
async def RequestAIResponseFunc(prompt, email, NumberOfRequestRetry, send_progress):  # Add send_progress
    """Generate AI text content asynchronously."""
    try:
        # Example progress update
        await send_progress(
            data={"status": "started", "details": "Processing your request..."},
            type='progress'
        )

        try:
            model = settings.AI_MODEL
            # Emit progress before starting work
            await send_progress(
                data={"status": "model_start", "details": "Initializing AI model..."},
                type='progress'
            )

            response = await asyncio.to_thread(model.generate_content, prompt)

        except Exception as e:
            # Send error progress
            await send_progress(
                data={"status": "error", "details": "Model failed to respond"},
                type='progress'
            )
            raise RetryCustomError("retry", "Issue with request. Try again later❌")

        # Emit progress during processing
        await send_progress(
            data={"status": "processing", "details": "Formatting response..."},
            type='progress'
        )

        cleaned_json_string = response.text.strip("```json\n").strip("```")
        json_data = json.loads(cleaned_json_string)

        # Final result (still sent via handle_request_ai_response)
        return {'type': 'success', 'status': 'success', 'result': json_data}

    except Exception as e:
        # Send error progress
        await send_progress(
            data={"status": "error", "details": str(e)},
            type='progress'
        )
        raise  # Re-raise for circuit breaker
```

### Key Changes:
1. **Progress Callback**: `send_progress` is passed to `RequestAIResponseFunc` and used to send intermediate messages.
2. **Progress Points**: Messages are sent at critical points (start, model init, processing, errors).
3. **Message Structure**: Use a consistent structure (e.g., `status` and `details` fields) for client-side handling.

### Client-Side Handling
On the client, listen for messages with `type: 'progress'`:

```javascript
websocket.addEventListener('message', (e) => {
    const data = JSON.parse(e.data);
    if (data.type === 'progress') {
        // Update UI with data.message.status/details
    } else if (data.type === 'RequestAIResponse') {
        // Handle final response
    }
});
```

### Notes:
- **Thread Safety**: `model.generate_content` runs in a thread, so progress messages before/after it are async-safe.
- **Error Handling**: Ensure errors are caught and progress messages are sent before re-raising.
- **Connection State**: The circuit breaker and task cancellation in `disconnect` will handle cleanup if the client disconnects mid-process.

This approach allows real-time feedback during long-running AI operations.